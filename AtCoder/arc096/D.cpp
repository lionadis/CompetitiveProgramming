#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
	int n;
	ll c;
	vector<pair<ll, ll> > a(n);
	cin >> n >> c;
	for(int i = 0; i < n; ++ i) cin >> a[i].first >> a[i].second;
	
}
